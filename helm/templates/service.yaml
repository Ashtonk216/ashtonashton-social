apiVersion: v1
kind: Service
metadata:
  name: social-frontend
  labels:
    app.kubernetes.io/name: social-frontend
    app.kubernetes.io/instance: social-frontend
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: http
  selector:
    app.kubernetes.io/name: social-frontend
    app.kubernetes.io/instance: social-frontend
